# Pinned to the exact versions verified working in production (2026-06-21).
# Captured via `pip freeze` in the running container so rebuilds are
# reproducible and a future release of a transitive dep can't silently break
# the image. Bump deliberately, then re-verify against the speakers.
#
# Direct dependencies: flask, gunicorn, soco, requests
appdirs==1.4.4
blinker==1.9.0
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.1.8
Flask==3.1.3
gunicorn==23.0.0
idna==3.18
ifaddr==0.2.0
importlib_metadata==8.7.1
itsdangerous==2.2.0
Jinja2==3.1.6
lxml==6.1.1
MarkupSafe==3.0.3
packaging==26.2
requests==2.32.5
soco==0.30.10
urllib3==2.6.3
Werkzeug==3.1.8
xmltodict==1.0.4
zipp==3.23.1
